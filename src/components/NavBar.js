import React from "react";
const element = (
  <>
  <nav>NavBar
    <a href="#home">Home</a>
    <a href="#about">About</a>
  </nav>
  </>
)
function NavBar() {
  // update the JSX being returned!
  return (
    element
  );
}

export default NavBar;
